# Blueprint Agent 요구사항 정의서

## 1. 프로젝트 개요

### 1.1 서비스 비전

회의에서 오간 말과 이미 존재하는 화면을, 사람이 눈으로 확인하고 확정하는 순간에 기계가 저비용으로 이해할 수 있는 계약(contract)으로 변환하여, 요구사항 명세 · 데이터 모델 · API 명세 · 실행 코드까지 근거를 잃지 않고 도출하는 **Requirement-to-Architecture 시스템**

### 1.2 해결하려는 문제

**문제 1 — 요구사항 오해로 인한 재작업**
소프트웨어 개발에서 가장 비싼 재작업은 "만들고 나서야 요구가 잘못 이해됐음을 발견하는 것"이다. 텍스트 요구사항은 이해관계자마다 다르게 읽히며, 그 차이는 구현이 끝난 뒤에야 드러난다.

**문제 2 — 사람용 산출물을 기계가 읽는 비용**
화면 목업은 *사람의 검토*를 위해 만들어진 산출물이다. 이것을 후속 자동화 단계가 원본 그대로 읽으면 입력 비용이 크고, 시각적 잡음이 판단 품질을 떨어뜨린다.

**문제 3 — 근거를 잃은 변경**
자동화된 도구가 산출물을 수정할 때 "왜 이렇게 바뀌었는가"에 답할 수 없으면, 변경을 검증할 방법이 없다. 요구의 출처가 소실되는 순간 결과물은 감사 불가능해진다.

### 1.3 핵심 가치 제안

**기획자 · 이해관계자 관점**
- 회의 중 나온 말이 즉시 눈으로 볼 수 있는 화면이 되어, 오해를 회의 자리에서 발견
- 화면을 보며 대화로 수정하고, 만족한 시점에 "확정"을 선언하는 것만으로 후속 명세가 도출됨
- 전문 문서 작성 능력 없이도 정식 요구사항 명세를 산출

**개발자 관점**
- 확정된 화면과 1:1로 대응하는 기계 판독 계약을 받으므로, 화면 해석에 대한 추측이 불필요
- 모든 요구사항 항목이 원래 발언까지 역추적 가능
- 이미 운영 중인 레거시 화면도 같은 표현 체계로 흡수되어, 신규·기존이 하나의 파이프라인에서 다뤄짐

**조직 관점**
- 자동화 단계의 입력 비용을 정량적으로 측정·증명 가능
- 요구 → 화면 → 계약 → 명세 → 코드의 전 구간에 근거 사슬이 남아 감사 가능

**시장 차별화**
- 화면 생성 도구가 아니라, **화면을 매개체로 쓰는 요구사항-아키텍처 도출 시스템**
- 신규 개발(그린필드)과 레거시 흡수(브라운필드)를 동일한 중간 표현으로 통합

### 1.4 시스템 불변 원칙

아래 원칙은 전 기능에 우선하며, 어떤 기능도 이를 위반해서는 안 된다.

**원칙 1 — 원본 화면 마크업 격리**
언어 모델은 화면 마크업 원본을 소비하지 않는다. 원본은 정해진 변환 지점에서 단 한 번만 접촉되고, 이후 모든 단계는 정규화된 중간 표현만 입력으로 받는다.

**원칙 2 — 확정 단계의 결정성**
화면을 계약으로 변환하는 단계는 언어 모델을 사용하지 않는다. 동일 입력은 언제나 동일 출력을 낳아야 하며, 이 단계의 모델 호출 비용은 0이어야 한다.

**원칙 3 — 근거 없는 생성 금지**
요구 브리프는 입력된 발언에 명시되지 않은 내용을 추가하지 않는다. 화면은 브리프에 명시되지 않은 요소를 임의로 생성하지 않는다. 추측에 의한 보완은 금지한다.

**원칙 4 — 근거 보존**
모든 산출물은 자신이 파생된 상위 산출물과 그 근거(회의 발언)를 식별 가능한 형태로 함께 보존한다.

---

## 2. 서비스 구성

### 2.1 주요 구성 요소

- **작업 콘솔**: 이해관계자와 개발자가 사용하는 단일 화면. 회의 대화 입력, 화면 미리보기, 산출물 열람, 단계별 실행을 담당
- **파이프라인 처리 계층**: 요구 캡처 → 화면 공동 설계 → 확정 및 계약 추출 → 명세 역도출 → 코드 생성의 순차 처리
- **산출물 저장소**: 프로젝트별 화면 버전 이력, 확정 시점의 계약, 도출된 명세, 근거 메타데이터를 영속 보관

### 2.2 처리 계층 정의

| 계층 | 입력 | 출력 | 모델 사용 |
|---|---|---|---|
| 요구 캡처 | 회의 녹취 텍스트 | 구조화된 요구 브리프 | 사용 |
| 화면 공동 설계 | 요구 브리프 | 의도 메타데이터가 부착된 단일 화면 | 사용 |
| 확정 및 계약 추출 | 확정된 화면 | 시각 계약 문서 | **미사용 (결정적)** |
| 명세 역도출 | 시각 계약 문서 | 요구사항 명세 · 데이터 모델 · API 명세 | 사용 |
| 코드 생성 | 도출된 명세 | 프론트엔드 · 백엔드 · 데이터베이스 코드 | 사용 |

---

## 3. 핵심 기능 요구사항

### 3.1 요구 캡처 기능

#### 3.1.1 회의 녹취 입력

**목적**: 회의에서 오간 발언을 시스템의 최초 입력으로 확보

**기능 요구사항**:
- 발언자 라벨이 붙은 텍스트 형식(`발언자: 내용`)의 녹취 입력
- 발언자 라벨이 없는 줄도 하나의 발언으로 수용
- 예시 녹취 템플릿 목록 제공 및 선택 시 즉시 로드
- 작업 중 발언을 추가하면 이후 처리에 반영

**UI/UX 요구사항**:
- 발언을 채팅 버블 형태로 시간순 표시
- 발언자별 시각적 구분

**데이터 관리**:
- 녹취 원문은 각 화면 버전 생성 시점의 스냅샷으로 보존

#### 3.1.2 실시간 음성 인식 (선택 기능)

**목적**: 회의 음성을 별도 전사 작업 없이 직접 입력으로 전환

**기능 요구사항**:
- 마이크 입력을 실시간으로 텍스트화하여 녹취에 반영
- 화자 분리를 수행하여 발언자 라벨을 자동 부여
- 한국어를 포함한 다국어 인식
- 발화 후 텍스트 반영까지 수 초 이내

**제약 요구사항**:
- 음성 데이터가 외부 서비스로 전송되지 않는 로컬 처리 방식이어야 한다
- 음성 인식 자원은 최초 1회 준비 절차로 설치하며, 미설치 시 이 기능만 비활성화되고 나머지 기능은 정상 동작해야 한다

#### 3.1.3 발언 식별 체계

**목적**: 개별 발언을 안정적으로 참조할 수 있는 식별자 부여

**기능 요구사항**:
- 각 발언에 문서 순서 기반의 식별자(`u-001` 형식)를 결정적으로 부여
- 동일한 녹취에 대해서는 언제나 동일한 식별자가 부여되어야 한다 (버전 간 식별자 안정성)
- 발언 추가 시 기존 발언의 식별자는 변경되지 않는다

**데이터 관리**:
- 식별자 부여는 모델 호출 없이 결정적으로 수행

#### 3.1.4 요구 브리프 추출

**목적**: 비정형 발언을 후속 단계가 소비할 수 있는 구조화된 요구 명세로 변환

**기능 요구사항**:
- 다음 세 영역으로 구조화된 브리프 산출
  - 화면 목적
  - 데이터 객체와 필드 (필드별 타입 및 표현 형태 포함)
  - 사용자 액션 (액션명, 대상 객체, 변경 필드, 결과값)
- 녹취에 명시되지 않은 내용은 추가하지 않는다 (§1.4 원칙 3)
- 브리프의 각 항목은 근거가 된 발언 식별자를 `[근거: u-002, u-005]` 형태로 태그한다
- 브리프는 사람이 읽을 수 있는 문서 형태를 유지하여, 후속 단계와 화면 표시에 동일한 형태로 전달된다

**처리 방식 요구사항**:
- 장시간 소요 가능한 작업이므로 비동기 실행 후 진행 상태를 조회할 수 있어야 한다

---

### 3.2 화면 공동 설계 기능

#### 3.2.1 화면 생성

**목적**: 요구 브리프를 이해관계자가 즉시 볼 수 있는 화면으로 전환

**기능 요구사항**:
- 요구 브리프로부터 단일 화면 산출물 생성
- 브리프에 명시된 요소만 생성하며, 명시되지 않은 요소를 임의로 추가하지 않는다 (§1.4 원칙 3)
- 생성된 화면의 모든 의미 요소에 **저자 의도 메타데이터**를 부착한다
  - 컴포넌트 식별자 및 역할 구분 (제목 / 배지 / 버튼 / 입력 등)
  - 데이터 바인딩 (어떤 객체의 어떤 필드를 표시하는가)
  - 액션 정의 (동사, 대상 객체, 변경 필드, 결과값)
- 메타데이터는 화면의 시각적 표현이 담을 수 없는 *의도*를 기계 판독 가능한 형태로 보존하기 위한 것이며, 후속 확정 단계의 필수 입력이다
- 생성 화면의 시각 규격(색·타이포·컴포넌트·간격·반경)은 `requirements/samsung-design-guidelines.md`를 따른다. 팔레트·반경·8px 간격 그리드 밖의 임의 값을 만들지 않는다

**UI/UX 요구사항**:
- 생성된 화면을 콘솔 내에서 즉시 미리보기
- 미리보기를 지정 해상도 기준 등비 스케일 전체화면으로 확대 가능
- 생성 진행 상태를 실시간 표시

#### 3.2.2 대화 기반 델타 수정

**목적**: 화면 전체를 재생성하지 않고, 추가된 발언만 반영하여 수정

**기능 요구사항**:
- 직전 버전 생성 시점의 대화 스냅샷 이후 **추가된 발언만** 델타로 추출
- 수정 시 입력은 **직전 버전 화면 + 델타 발언에서 도출된 수정 브리프**로 한정한다
- 근거가 되는 발언 없이는 수정이 발생할 수 없는 구조여야 한다
- 수정 결과는 새 버전으로 기록하며, 생성 모드(신규/수정)와 부모 버전을 함께 남긴다

**UI/UX 요구사항**:
- 수정 모드로 동작 중임을 시각적으로 구분 표시

#### 3.2.3 버전 이력 관리

**목적**: 화면의 변천 과정과 그 근거를 언제든 검증 가능하게 보존

**기능 요구사항**:
- 프로젝트별 · 버전별 화면 산출물 영속화
- 버전 목록 조회 및 과거 버전 재로드·미리보기
- 버전 간 비교를 위한 메타데이터 제공

**데이터 관리** (버전마다 보존해야 하는 항목):
- 생성 시각
- 생성 모드 (신규 생성 / 수정)
- 부모 버전 식별자
- 해당 시점의 대화 스냅샷 전문
- 이 버전의 근거 발언 목록 (신규 생성 = 전체 발언, 수정 = 델타 발언만)
- 요구 브리프 요약
- 모델 사용량 (입력·출력 토큰)

---

### 3.3 확정 및 계약 추출 기능

#### 3.3.1 시각적 확정 (Freeze)

**목적**: 사람의 검토가 끝난 시점을 명시적 경계로 삼아, 이후 자동화의 기준점을 고정

**기능 요구사항**:
- 사용자가 특정 화면 버전에 대해 "확정"을 선언
- 확정 시점의 타임스탬프와 함께 산출물 일체를 별도 실행 단위로 보존
- 확정 이후의 후속 단계는 확정된 계약만을 입력으로 삼는다

#### 3.3.2 시각 계약 추출

**목적**: 확정된 화면을 기계가 저비용으로 이해하는 계약 문서로 변환

**기능 요구사항**:
- **언어 모델을 사용하지 않고** 결정적으로 추출한다 (§1.4 원칙 2)
- 세 가지 정보원을 융합하여 추출
  - 기하 정보: 렌더링 결과에서 계산된 위치·크기·스타일
  - 구조 정보: 문서 구조에서 도출된 요소 역할
  - 의도 정보: §3.2.1에서 부착된 저자 의도 메타데이터
- 계약 문서는 사람이 읽을 수 있고 기계가 파싱할 수 있는 구조화 형식이어야 한다

**계약 문서 필수 구성**:
- 페이지 정보 (제목, 기준 뷰포트 크기)
- 데이터 객체 목록 (객체 식별자, 필드명, 타입, 표현 형태)
- 컴포넌트 목록 (식별자, 역할, 경계 상자, 데이터 바인딩)
- 액션 목록 (식별자, 역할, 라벨, 경계 상자, 트리거 시 동작 정의)
- 상태 흐름 (어떤 액션이 어떤 상태 전이를 유발하는가)

**성능 요구사항**:
- 이 단계는 동기 처리로 즉시 응답 가능해야 한다
- 모델 호출 비용 0

---

### 3.4 명세 역도출 기능

#### 3.4.1 명세 도출

**목적**: 시각 계약 하나로부터 개발에 필요한 세 종류의 정식 명세를 도출

**기능 요구사항**:
- **입력은 시각 계약 문서로 한정한다.** 원본 화면 마크업이나 녹취 원문을 입력으로 받지 않는다 (§1.4 원칙 1)
- 다음 세 산출물을 도출
  - **요구사항 명세(SRS)**: 계약의 액션과 상태 흐름에서 도출
  - **데이터 모델(ERD)**: 계약의 데이터 객체와 참조 관계에서 도출
  - **API 명세(OpenAPI)**: 계약 액션의 동사와 대상에서 도출
- 요구사항 명세의 기능 요구사항 항목은 `FR-01` 형식의 체계적 번호를 부여하여, 명세 간 상호 참조가 가능해야 한다

**UI/UX 요구사항**:
- 도출된 명세를 표와 코드 블록을 포함한 서식 문서로 렌더링하여 콘솔에서 열람 가능

**처리 방식 요구사항**:
- 비동기 실행 후 진행 상태 조회

---

### 3.5 코드 생성 기능

**목적**: 도출된 명세가 실제 실행 가능한 코드로 이어짐을 실증

**기능 요구사항**:
- 도출된 명세로부터 3계층 코드 산출
  - 프론트엔드 화면 컴포넌트 — 시각 규격은 `requirements/samsung-design-guidelines.md`의 토큰과 Tailwind 유틸리티 매핑을 따른다 (a11y 대비 4.5:1 · focus ring 규칙은 간소화 대상 아님)
  - 백엔드 라우트 (API 명세의 경로·동사 준수)
  - 데이터베이스 스키마 정의
- 생성된 코드는 근거가 된 요구사항 항목 번호를 주석으로 참조하여, 코드에서 명세로 역추적이 가능해야 한다

**범위 요구사항**:
- 실행 가능한 완전한 애플리케이션이 아니라, 명세 → 코드 연결의 실증을 목적으로 하는 대표 파일 집합을 산출한다

---

### 3.6 추적성 기능

**목적**: 모든 요구와 변경의 출처를 회의 발언으로 환원

**기능 요구사항**:
- 회의 발언을 근거 원천으로 시작하는 추적 사슬을 유지한다 (누가 · 언제 · 무엇을)
- 사슬의 각 단계는 상위 산출물과 근거를 함께 보존한다
  ```
  회의 발언 (발언 ID 부여)
   └─ 요구 브리프 (항목별 [근거: u-XXX] 태그)
       └─ 화면 버전 vN (생성 시각·모드·부모 버전·대화 스냅샷·근거 발언 목록)
           └─ 시각 계약 (확정 시점 고정)
               └─ 요구사항 명세(FR-XX) · 데이터 모델 · API 명세
                   └─ 코드 (요구사항 항목 참조 주석)
  ```
- 시스템은 다음 질문 각각에 산출물만으로 답할 수 있어야 한다

| 질문 | 답을 주는 근거 |
|---|---|
| 언제 이 요구가 들어왔는가 | 버전 생성 시각 + 확정 타임스탬프 |
| 누가 요청했는가 | 버전별 대화 스냅샷의 발언자 라벨 |
| 무엇이 바뀌었는가 | 직전 스냅샷 대비 델타 발언 + 생성 모드 + 부모 버전 |
| 왜 바뀌었는가 | 델타 발언 → 수정 브리프 → 반영 결과의 연결 |
| 자동화가 수정 시 근거를 보는가 | 수정 모드의 입력이 직전 버전 + 근거 브리프로 구조적으로 한정됨 |
| 과잉 변경을 막는가 | §1.4 원칙 3의 생성 금지 규칙 |

---

### 3.7 작업 콘솔 기능

> **화면 구성 정본**: 이 절은 콘솔의 *기능 요구*를 정의하고, **레이아웃 · 뷰 구성 · 상태(state) · SSE 이벤트 · 키보드 단축키 · 백엔드 엔드포인트 계약의 상세 정본은 `requirements/redev-screen-spec.yaml`이다.** 콘솔을 구현·재개발할 때 화면 구조의 값·이벤트·상태명은 해당 YAML을 1:1로 따른다.
>
> **디자인 규격 정본**: 콘솔 및 모든 웹 산출물(생성 목업 §3.2.1, 생성 프론트엔드 코드 §3.5 포함)의 색·타이포·컴포넌트·간격 규격 정본은 `requirements/samsung-design-guidelines.md`이다.
>
> **범위**: `redev-screen-spec.yaml`은 이번 MVP 범위에 맞춰 정리되어 있다(레거시 흡수·비용 증명 관련 뷰/상태/엔드포인트 제거 완료). 개발자 모드 뷰는 `mockup · brief · contract · spec · code · versions` 6종이다.

#### 3.7.1 이해관계자 모드 (기본 화면)

**목적**: 전문 지식 없는 사용자가 최소 조작으로 결과에 도달

**기능 요구사항**:
- 단일 실행 버튼으로 다음이 자동 연속 실행
  1. 요구 정리 (요구 브리프 추출)
  2. 화면 생성
  3. 명세 도출
- 좌측: 회의 녹취를 채팅 형태로 표시하고 발언 추가 가능
- 우측: 산출물 전환 토글 (화면 미리보기 / 명세 문서)
- 발언 추가 후 재실행 시 §3.2.2 델타 수정으로 동작

**UI/UX 요구사항**:
- 기본 진입 화면이 이 모드여야 한다
- 단계 진행 상황을 실시간 표시

#### 3.7.2 개발자 모드

**목적**: 파이프라인 각 단계를 개별 실행하고 중간 산출물을 검사

**기능 요구사항**:
- 단계별 개별 실행: 요구 캡처 → 화면 생성 → 확정 → 명세 도출 → 코드 생성
- 중간 산출물 검사 탭 제공
  - 요구 브리프
  - 시각 계약 문서
  - 도출 명세
  - 생성 코드
  - 버전 이력

**UI/UX 요구사항**:
- 이해관계자 모드와 개발자 모드를 전환 가능
- 전환 수단은 버튼 · 단축키 · 주소 파라미터의 세 경로 제공

#### 3.7.3 실행 상태 관리

**목적**: 장시간 소요되는 처리 중에도 사용자가 진행을 인지

**기능 요구사항**:
- 모델을 사용하는 모든 처리는 **비동기 실행**으로 즉시 작업 식별자를 반환하고, 상태를 별도 조회 가능해야 한다
- 모델을 사용하지 않는 결정적 처리(확정·계약 추출)는 **동기 처리**로 즉시 응답한다
- 처리 진행 상황을 서버에서 클라이언트로 **실시간 스트리밍**하여 단계별 완료를 즉시 반영한다
- 스트리밍 대상 이벤트: 요구 브리프 / 화면 / 계약 / 명세 / 코드
- 실패 시 오류 사유를 사용자에게 표시하고, 이전 상태를 보존한다

---

## 4. MVP 개발 범위

### 4.1 필수 (핵심 파이프라인)

**목표**: 회의 발언 하나로 명세까지 도달하는 전 구간 실증

- 회의 녹취 입력 및 발언 식별 체계 (§3.1.1, §3.1.3)
- 요구 브리프 추출 — 근거 태그 포함 (§3.1.4)
- 화면 생성 — 의도 메타데이터 부착 (§3.2.1)
- 대화 기반 델타 수정 (§3.2.2)
- 버전 이력 관리 (§3.2.3)
- 시각적 확정 및 계약 추출 — 결정적 (§3.3)
- 명세 역도출 — 요구사항 명세 · 데이터 모델 · API 명세 (§3.4)
- 작업 콘솔 이해관계자 모드 + 개발자 모드 (§3.7)

### 4.2 필수 (차별화 실증)

- 코드 생성 — 명세 → 코드 연결 실증 (§3.5)

### 4.3 후순위

- 실시간 음성 인식 (§3.1.2) — 미설치 시 나머지 기능이 정상 동작해야 하므로 독립 배치 가능

---

## 5. 개발 작업 방식 요구사항

**목적**: 이 MVP는 AI-DLC 파이프라인을 다중 에이전트 플릿으로 병렬 개발한다. 작업 방식 자체를 근거 사슬(§3.6)과 동일한 원칙 — 누가·무엇을·어떤 근거로 — 아래 두어, 산출물이 어느 인스턴스의 어떤 작업에서 나왔는지 역추적 가능하게 한다.

### 5.1 팀 구성

- 작업자는 총 **5명**: 오케스트레이터 1 + 워커 인스턴스 4
- **메인 오케스트레이터 겸 PM (jd, 로컬 세션)**: 조율·통합·게이트·판단을 담당한다. inception·계획·계약 수정 권한을 단독 보유한다
- **워커 인스턴스 4대**: `suman` · `mingu` · `gyu` · `yudo`
- **백업 (`ardr-dev`)**: 전면 배치에서 제외하며, 지정된 백업 조건에서만 투입한다
- 각 인스턴스의 자원 등급 · 배분 무게 · 승계 순서 등 세부 정보의 **정본은 `/blue-agents` 스킬**이다. 이 문서는 정본을 참조하며 중복 서술하지 않는다

### 5.2 역할 분리 — 오케스트레이터가 계약을 소유한다

- **메인 오케스트레이터(jd)** 가 다음을 단독으로 확정한다. 이것이 병렬 작업의 공유 인터페이스(계약)다.
  - 설계 (컴포넌트·모듈 경계와 의존 관계)
  - 데이터 계약 (슬라이스 간 공유되는 데이터 모델·타입)
  - API 명세 (경로·동사·요청/응답 형태)
- 워커 인스턴스는 계약을 **소비**할 뿐 수정하지 않는다. 계약 변경이 필요하면 오케스트레이터에 **제안**만 하고, 반영 여부는 오케스트레이터가 결정한다

### 5.3 브랜치 모델

- `main` — 통합 대상. 동결된 설계·계약의 정본
- `jd` — 오케스트레이터 전용. 설계·계약·API 명세 수정은 이 브랜치에서만 하고, 확정 시 `main`에 머지한다
- `feat/<worker>` — 슬라이스별 1개. 항상 최신 `main`에서 분기한다

### 5.4 원자적 슬라이스 병렬 프로세스

**핵심 원칙**: 오케스트레이터가 계약을 확정한 뒤 구현을 **정확히 4개의 원자적 슬라이스**로 분해하여 워커 인스턴스 4대에 1:1 배분한다. 각 슬라이스는 계약 동결 이후 다른 슬라이스의 완료를 기다리지 않고 독립적으로 완결 가능해야 한다(원자성). 슬라이스 경계와 매핑은 AI-DLC inception(Units Generation)이 정하며 고정이 아니다.

**수명주기 (상태 전이)**:

```
[1] 계약 확정   오케스트레이터가 설계·데이터 계약·API 명세를 확정 (jd → main)
      │
[2] 동결·배리어  4개 슬라이스 전부 계약 ACK(READY) 이후에만 구현 시작.
      │          한 슬라이스라도 ACK 전이면 전체 대기.
      ▼
[3] 병렬 구현    4개 슬라이스를 4개 인스턴스가 동시 진행. 상호 대기 없음.
      │
      ├─ 진행 불가 선언(HOLD) ─────────────────────────┐
      │   어느 슬라이스든 계약 결함으로 진행 불가를        │
      │   선언하면 **전체 작업을 즉시 일시정지**한다.      │
      │   오케스트레이터가 계약을 개정(jd 브랜치)하고      │
      │   재동결한 뒤 [2] 배리어부터 재개한다.            │
      │                                                │
      │◄───────────────────────────────────────────────┘
      ▼
[4] 완료         이슈 없는 슬라이스는 끝까지 진행하여 각자 완결
      │
[5] 통합         전 슬라이스 완료 후 오케스트레이터가 `feat/<worker>`를
                 `main`으로 통합하고 게이트를 실행한다
```

**규칙 요구사항**:
- **전역 일시정지**: 슬라이스 하나의 진행 불가 선언은 개별 슬라이스가 아니라 **전체 작업**을 멈춘다. 계약이 개정되면 전 슬라이스가 재동결된 계약으로 다시 시작하거나 이어간다
- **근거 없는 진행 금지**: 계약이 동결되지 않은 상태에서는 어떤 슬라이스도 구현을 시작하지 않는다 (§1.4 원칙 3의 작업 방식 대응물)
- **통합 시점 일원화**: 워커는 자신의 슬라이스만 완결하고, `main` 통합·게이트는 오케스트레이터만 수행한다
- 상세한 정지-수정-재개 규율은 `parallel-atomic-dev` 스킬을, 인스턴스 배분·승계·하달 절차는 `/blue-agents`(정본)를 따른다

### 5.5 작업 추적

- 한 슬라이스는 한 인스턴스가 담당한다. 같은 슬라이스를 두 인스턴스에 걸치지 않는다
- 인스턴스 이탈 시 승계 순서(`gyu → yudo → ardr-dev`)에 따라 살아있는 인스턴스가 해당 `feat/<worker>` 브랜치를 이어받는다
- 취합 보고는 **어느 인스턴스가 어떤 슬라이스를 수행했는지, 전역 정지 발생 여부와 사유, 승계 여부**를 명시한다 (작업 방식의 추적성)

---

## 부록

### A. 동반 입력 문서 (AI-DLC 입력 세트)

이 요구사항 정의서는 다음 문서와 **함께** AI-DLC의 입력이 된다. 상충 시 우선순위는 §C를 따른다.

| 문서 | 역할 | 관계 |
|---|---|---|
| `blueprint-agent-requirements.md` (본 문서) | 기능·작업 방식 요구의 정본 | 범위(scope)의 최종 권위 |
| `blueprint-agent-constraints.md` | 제외 기능 · 설계 제약(C-1~C-4) | 본 문서 §1.4 · §5와 대응 |
| `requirements/redev-screen-spec.yaml` | 콘솔 화면 구성(레이아웃·뷰·상태·SSE·단축키·엔드포인트)의 상세 정본 | §3.7이 참조. MVP 범위에 맞춰 정리됨 |
| `requirements/samsung-design-guidelines.md` | 모든 웹 산출물의 시각 디자인 규격 정본 | §3.2.1 · §3.5 · §3.7이 참조 |

### B. 용어 정의

- **요구 브리프(Brief)**: 비정형 회의 발언을 화면 목적 · 데이터 객체 · 사용자 액션의 세 영역으로 구조화한 중간 산출물
- **저자 의도 메타데이터**: 생성된 화면의 각 요소에 부착되는 기계 판독용 정보. 컴포넌트 역할, 데이터 바인딩, 액션 정의를 담으며, 픽셀이 표현할 수 없는 *의도*를 보존한다
- **확정(Freeze)**: 사람의 검토가 끝났음을 명시적으로 선언하는 시점. 이 시점 이후의 자동화는 확정된 계약만을 기준으로 삼는다
- **시각 계약(Visual Contract)**: 확정된 화면을 기하 · 구조 · 의도의 세 정보원으로부터 결정적으로 추출한 기계 판독 문서. 후속 명세 도출의 유일한 입력
- **델타 수정**: 화면 전체를 재생성하지 않고, 직전 버전 생성 시점 이후 추가된 발언만 반영하여 수정하는 방식
- **근거 사슬(Traceability Chain)**: 회의 발언에서 시작하여 코드까지 이어지는, 각 단계가 상위 근거를 보존하는 파생 관계
- **발언 식별자**: 각 발언에 문서 순서 기반으로 부여되는 `u-001` 형식의 안정적 식별자
- **기능 요구사항 번호**: 도출된 요구사항 명세의 각 항목에 부여되는 `FR-01` 형식의 체계적 번호

### C. 상충 시 우선순위

기능 요구사항이 §1.4 시스템 불변 원칙과 상충하는 경우, **불변 원칙이 우선한다.** 특히 다음을 타협해서는 안 된다.

1. 확정·계약 추출 단계에 모델을 도입하여 결정성을 훼손하는 설계
2. 명세 역도출 단계에 원본 화면 마크업을 입력으로 추가하는 설계
3. 근거 없는 보완 생성을 허용하여 추적 사슬을 끊는 설계



---

## 프로젝트 평가 기준

[AI-DLC 활용도]
문서를 얼마나 많이 냈는가가 아니라, AI와의 작업이 실제로 결과물을 만들어냈다는 증거를 봅니다.
단계별 산출물이 서로 이어지는지, 앞 단계의 결정이 뒤에 반영됐는지가 기준입니다.
어떤 도구를 썼는지는 점수와 무관합니다.

[문제 정의 및 해결]
"누구의 어떤 문제를 어떻게 푸는가"가 읽는 사람에게 그대로 전달되는지를 봅니다.
언제, 얼마나 자주 불편한지와 대상 사용자가 누구인지가 구체적일수록 높습니다.
아이디어의 크기나 시장성은 여기서 보지 않습니다.

[창의성]
기존 도구와의 구조적 차별점이 코드나 설계 문서로 확인되는지를 봅니다.
"새롭다"는 주장만으로는 점수가 없습니다.
반대로 흔한 분야라도 구조가 실제로 다르면 인정합니다.

[완성도]
만들려던 것이 코드로 실제로 동작하는지를 봅니다.
동작하는 화면을 담은 시연 스크린샷을 screenshots/ 또는 result/ 에 꼭 넣어주세요.

· 빌드·실행 가능성 — 의존성 선언, 진입점, 락파일, CI 설정
· 에러 핸들링 — 주요 경로의 예외 처리와 전역 핸들러
· 의도한 흐름의 코드상 완결성 — 진입점부터 호출이 실제 구현까지 닿는지
· 실행 증거와 기능의 정합 — 스크린샷 속 화면이 README 의 기능과 맞는지
· 미완성·스텁 코드 — TODO 나 빈 함수가 핵심 경로에 남아 있는지

[사용성]
처음 보는 사람이 시작까지 막히지 않고 가는지, 그리고 화면 앞에 앉았을 때
다음에 무엇을 해야 할지 알 수 있는지를 봅니다.
문서가 있는가가 아니라, 그대로 따라가서 막히지 않는가가 기준입니다.

결과물이 웹 서비스라면 화면 자체가 사용법을 설명해야 하고, 라이브러리라면
API와 예제가 그 역할을 합니다. 형태에 맞춰 봅니다.

· 시작하기까지의 경로 — 설치·실행·필요한 키와 계정
· 사용 매뉴얼 — 실행한 뒤 무엇을 어떻게 하는지, 예제와 트러블슈팅
· UI 직관성 — 화면을 보면 다음 행동을 알 수 있는지
· 인터랙션 피드백 — 누른 뒤에 무슨 일이 일어났는지 알려주는지
(로딩·성공·오류 표시, 데이터가 없을 때의 빈 화면)
· 사용자 흐름의 완결성 — 핵심 시나리오 하나를 끝까지 통과할 수 있는지

[유지 보수성 및 보안]
후속 개발자가 이어받았을 때 곤란해지는 지점이 있는지를 봅니다.
해커톤 산출물임을 감안해 프로덕션 수준을 요구하지는 않습니다.

· 코드 구조·모듈화 · 설정 분리
· 시크릿 관리 — 키가 코드에 박혀 있지 않은지
· 인증·인가 및 입력 검증
· 로깅·관측 가능성