# U-content3 rev3 — Tasks

담당: `docs/**` · `aidlc-docs/construction/content-rev3/**` 만. README·fixtures·src·dashboard 미수정. git/npm 미실행.

## 문서
- [x] `content-rev3/design.md` 작성 (R1·R2·R3 반영 설계)
- [x] `content-rev3/tasks.md` 작성 (본 파일)

## docs/demo-runbook.md
- [x] R3 — 파트 1에 발언자 필드 시연(실명 부착·최근 화자 기억) 한 줄 추가
- [x] R3 — 생성 각주에 서버·로컬 reconcile(대화 어긋남 방지) 언급 추가
- [x] R2 — 파트 1 생성에 "N개 변형 생성 → `v1-1·v1-2` 나열" 시연 추가
- [x] R2 — 파트 3 버전 선택에 `v{group}-{variant}` 형제 선택 서술 추가
- [x] R1 — 파트 3에 경계선(`── vN 근거 경계 ──`) 클릭 되감기 + export 이력 경고(force) 시연 추가

## audit
- [x] `aidlc-docs/audit.md`에 `## U-content3 rev3` append

## 담당 밖 (참고)
- [ ] 콘솔 UI(revert·변형그룹·발언자필드·await·reconcile) → console subagent
- [ ] 계약(`/mockup {variants}`·`/meetings/:id/revert`, types/store) → jd 배리어
- [ ] README rev3 → 문서 팀원(동시 작업)
